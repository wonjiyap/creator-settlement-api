spring:
  application:
    name: creator-settlement-api
  datasource:
    url: jdbc:h2:mem:settlement
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  jackson:
    property-naming-strategy: SNAKE_CASE

settlement:
  fee-rate: 0.20

springdoc:
  swagger-ui:
    path: /api-docs
